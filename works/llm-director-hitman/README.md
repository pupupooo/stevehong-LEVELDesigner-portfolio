# LLM Director Hitman Work Page

## Structure

- `index.html`: standalone portfolio article for `LLM Director Hitman`.
- `images/`: copied presentation assets from `/Users/mofashu/Documents/llm_director_hitman`.

## Asset Naming

Use semantic filenames copied from the source project when possible. If replacing an image, keep the name tied to the represented route, map, or interaction state rather than to a timestamp.

## Cleanup

Remove unused copied assets when the page no longer references them.
